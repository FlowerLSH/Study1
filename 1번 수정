def vkdlsem(list,num):
    first = 0
    last = len(list)

    while first<=last:
        mid = (first + last) // 2
        if num < list[mid]:
                last = mid - 1
        elif num > list[mid]:
                first = mid + 1
        else:
            return mid
    return -1
